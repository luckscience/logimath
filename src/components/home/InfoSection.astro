<section
  class="info-section h-screen w-screen flex items-center justify-center scroll-smooth overflow-hidden"
  id="info"
>
  <div class="relative flex flex-col text-center items-center w-full h-full justify-center">
    
    <h1 class="text-white text-7xl mb-20">
      RUTA DE OBJETIVOS CON FLECHAS
    </h1>

    <!-- CONTENEDOR -->
    <div class="relative w-full max-w-4xl h-[600px]">

      <!-- SVG conexiones -->
      <svg class="absolute top-0 left-0 w-full h-full pointer-events-none">
        <defs>
          <marker id="arrow" markerWidth="10" markerHeight="10" refX="8" refY="3" orient="auto">
            <path d="M0,0 L0,6 L9,3 z" fill="white"/>
          </marker>
        </defs>

        <!-- conexiones curvas -->
        <path d="M400 80 C400 150, 250 200, 200 260" class="linea"/>
        <path d="M400 80 C400 150, 550 200, 600 260" class="linea"/>

        <path d="M200 260 C200 330, 300 380, 350 450" class="linea"/>
        <path d="M600 260 C600 330, 500 380, 450 450" class="linea"/>
      </svg>

      <!-- NODO RAÍZ -->
      <div class="nodo" style="top: 40px; left: 50%; transform: translateX(-50%)">
        <div class="circulo">🚀</div>
        <p class="texto">Inicio</p>
      </div>

      <!-- NIVEL 1 -->
      <div class="nodo izquierda" style="top: 250px; left: 200px;">
        <div class="circulo">📘</div>
        <p class="texto">Aprender</p>
      </div>

      <div class="nodo derecha" style="top: 250px; right: 200px;">
        <div class="circulo">🛠</div>
        <p class="texto">Practicar</p>
      </div>

      <!-- NIVEL 2 -->
      <div class="nodo" style="top: 440px; left: 350px;">
        <div class="circulo">💡</div>
        <p class="texto">Idea</p>
      </div>

      <div class="nodo" style="top: 440px; right: 350px;">
        <div class="circulo">🏁</div>
        <p class="texto">Meta</p>
      </div>

    </div>
  </div>
</section>

<style>
/* líneas */
.linea {
  fill: none;
  stroke: white;
  stroke-width: 2;
  stroke-dasharray: 400;
  stroke-dashoffset: 400;
  animation: draw 2s ease forwards;
  marker-end: url(#arrow);
}

@keyframes draw {
  to {
    stroke-dashoffset: 0;
  }
}

/* nodos */
.nodo {
  position: absolute;
  display: flex;
  flex-direction: column;
  align-items: center;
  color: white;
  opacity: 0;
  animation: fadeIn 0.6s ease forwards;
}

/* animación escalonada */
.nodo:nth-of-type(2) { animation-delay: 0.3s; }
.nodo:nth-of-type(3) { animation-delay: 0.6s; }
.nodo:nth-of-type(4) { animation-delay: 0.9s; }
.nodo:nth-of-type(5) { animation-delay: 1.2s; }

/* círculo */
.circulo {
  width: 50px;
  height: 50px;
  border-radius: 50%;
  background: rgba(255,255,255,0.1);
  border: 2px solid white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  margin-bottom: 8px;
  transition: all 0.3s ease;
}

/* hover interacción */
.nodo:hover .circulo {
  background: white;
  color: black;
  transform: scale(1.2);
  box-shadow: 0 0 20px rgba(255,255,255,0.6);
}

.nodo:hover .texto {
  transform: translateY(5px);
}

/* texto */
.texto {
  font-size: 0.9rem;
  transition: all 0.3s ease;
}

/* animaciones */
@keyframes fadeIn {
  to {
    opacity: 1;
  }
}
</style>