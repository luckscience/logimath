<section class="exercises-section h-screen text-center w-screen grid-pattern flex flex-col items-center py-10">

  <!-- 🔍 Buscador + filtros -->
  <div class="w-[85%] mt-20 mb-6 flex flex-col md:flex-row gap-4 justify-between items-center">

    <input 
      id="search-input"
      type="text"
      placeholder="Buscar ejercicio..."
      class="w-full md:w-1/3 px-4 py-2 rounded-lg bg-[#0e1638] border border-gray-400/20 text-white focus:outline-none focus:border-cyan-400 transition-all duration-300"
    />

    <div class="flex gap-3">
      <button class="filter-btn active" data-filter="all">Todos</button>
      <button class="filter-btn" data-filter="Principiante">Principiante</button>
      <button class="filter-btn" data-filter="Intermedio">Intermedio</button>
      <button class="filter-btn" data-filter="Avanzado">Avanzado</button>
    </div>

  </div>

  <!-- 🧱 Grid -->
  <div id="cards-container" class="w-[85%] grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6"></div>

</section>

<style is:global>
/* =========================
   CARD PRINCIPAL
========================= */
.exercise-card {
  font-family: inherit;
  padding: 1.5rem;
  background: #0e1638;
  border: 1px solid rgba(156, 163, 175, 0.2);
  border-radius: 0.75rem;

  display: flex;
  flex-direction: column;
  height: 100%;
  cursor: pointer;

  position: relative;

  transition: all 0.3s ease;
}

.exercise-card:hover {
  transform: translateY(-6px);
  box-shadow: 0 10px 30px rgba(34, 211, 238, 0.2);
  border-color: rgba(34, 211, 238, 0.4);
}

/* =========================
   TÍTULO
========================= */
.card-title {
  font-size: 1.25rem;
  font-weight: 700;
  color: white;
  margin-bottom: 2rem;

  transition: color 0.3s ease;
}

.exercise-card:hover .card-title {
  color: #67e8f9;
}

/* =========================
   TAGS CONTAINER
========================= */
.card-tags {
  display: flex;
  gap: 0.75rem;
  margin-top: auto;
}

/* =========================
   BADGES BASE
========================= */
.badge {
  font-size: 0.75rem;
  font-weight: 700;

  padding: 0.25rem 0.75rem;
  border: 1px solid;
  border-radius: 0.25rem;

  transition: transform 0.2s ease;
}

.exercise-card:hover .badge {
  transform: scale(1.05);
}

/* =========================
   VARIANTES BADGES
========================= */
.badge-yellow {
  border-color: #F4E13A;
  color: #F4E13A;
}

.badge-gray {
  border-color: #9ca3af;
  color: #d1d5db;
}

.badge-red {
  border-color: #f87171;
  color: #f87171;
}

.badge-green {
  border-color: #86efac;
  color: #86efac;
}

/* =========================
   BOTONES DE FILTRO
========================= */
.filter-btn {
  padding: 0.25rem 0.75rem;
  border: 1px solid rgba(156, 163, 175, 0.2);
  border-radius: 0.375rem;

  font-size: 0.875rem;
  color: #d1d5db;

  background: transparent;

  transition: all 0.2s ease;
  cursor: pointer;
}

.filter-btn:hover {
  border-color: #22d3ee;
  color: #67e8f9;
}

.filter-btn.active {
  background: rgba(34, 211, 238, 0.1);
  border-color: #22d3ee;
  color: #67e8f9;
}
</style>

<script type="module" src="/src/scripts/exercises.ts"></script>